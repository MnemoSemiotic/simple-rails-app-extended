<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :student_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :student_last_name %>
  </div>

  <div class="field">
    <%= form.label "Student Number" %>
    <%= form.number_field :student_id, id: :student_student_id %>
  </div>

  <div class="field">
    <%= form.label "Number of Credits Completed" %>
    <%= form.number_field :credit_number, id: :student_credit_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
